/* ROOT: --------------------------------------------------------------------------------------- */

.gc-character-kernel-root {
    margin-top: var(--gc-common-space-smaller);
}

.gc-character-kernel-body {
    
}